 import {Meteor} from 'meteor/meteor';
import {players} from './../imports/api/players';

Meteor.startup(() =>  
{
 
 
 /* class Person {
    constructor(name ='Anonymous',age=0){
      this.name = name;
      this.age = age;
    }
    getGreeting(){
      return 'Hi! I am ${this.name}.';
    }
    getPersonDescription() {
      return '${this.name} is ${this.age} years old.';
    }
  }
  class Employee extends Person{
    constructor(name, age ,title){
      super(name,age);
      this.title= title;
    
    }
    getGreeting() {
      if (this.title) {
        return `Hi! Iam ${this.name}. I work as a ${this.age}.`;
    }else {
      return super.getGreeting();
    }
  }
  hasjob() {
    return !! this.title;
      }
    }
    class Programmer extends Person {
      constructor(name,age,PreferredLanguage='assembly'){
      super(name,age);
      this.PreferredLanguage =PreferredLanguage;
      }
    
    getGreeting(){
      return `Hi I am ${this.name}. I am a ${this.PreferredLanguage}`;
     }
    }
    let userOne = new Programmer();
    console.log(userOne.getGreeting());
    let me = new Person('nour',26,'db admin');
    console.log(me.getGreeting());


    //let Person = new Person('andrew');
    //console.log(Person.getPersonDescription());
  //new Person('norhan',23)


//function square(x){
  //  return x*x;

//};
//console.log(square(10));
//let square = (x)=> {
  //  return x*x;
//};
//console.log (square(3));
//let user = {
  //  name: 'andrew',
//}

*/
});

